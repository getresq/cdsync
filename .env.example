# Local integration-test defaults. Copy to .env for local runs.
# CI should inject these as environment variables or secrets instead.

# Postgres used by ignored e2e tests.
CDSYNC_E2E_PG_URL=postgres://cdsync:cdsync@localhost:5433/cdsync

# BigQuery emulator used by ignored local e2e tests.
CDSYNC_E2E_BQ_HTTP=http://localhost:9050
CDSYNC_E2E_BQ_GRPC=localhost:9051
CDSYNC_E2E_BQ_PROJECT=cdsync
CDSYNC_E2E_BQ_DATASET=cdsync_e2e

# Real BigQuery used by ignored live e2e tests.
CDSYNC_RUN_REAL_BQ_TESTS=0
CDSYNC_REAL_BQ_PROJECT=your-gcp-project
CDSYNC_REAL_BQ_DATASET=cdsync_e2e_real
CDSYNC_REAL_BQ_LOCATION=US
CDSYNC_REAL_BQ_BATCH_LOAD_BUCKET=your-cdsync-load-bucket

# Keep the real service-account key out of git.
CDSYNC_REAL_BQ_KEY_PATH=.secrets/your-service-account.json

# Admin API service-to-service JWT verification (RS256).
# Expected format: JSON object mapping `kid` -> PEM public key.
# Example value:
# CDSYNC_SERVICE_JWT_PUBLIC_KEYS_JSON={"your-issuer-local-20260331-01":"-----BEGIN PUBLIC KEY-----\\n...\\n-----END PUBLIC KEY-----"}
CDSYNC_SERVICE_JWT_PUBLIC_KEYS_JSON=
# Comma-separated issuers allowed to call the admin API.
CDSYNC_SERVICE_JWT_ALLOWED_ISSUERS=
CDSYNC_SERVICE_JWT_ALLOWED_AUDIENCES=cdsync
